# Core modules
faster-whisper
psutil
requests
numpy
python-dotenv

# STT and audio
whispercpp           # or leave as placeholder if using subprocess to call binary
SpeechRecognition
pyaudio
sounddevice          # or pyaudio or alsaaudio depending on input method
soundfile
webrtcvad

# TTS
piper-tts
pyttsx3              # optional backup TTS engine

# Optional: Web/API
flask                # if adding API endpoint later

# Optional: tooling
setuptools
wheel
